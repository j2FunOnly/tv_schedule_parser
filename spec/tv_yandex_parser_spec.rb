require "spec_helper"

describe TvYandexParser do
  it "has a version number" do
    expect(TvYandexParser::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
